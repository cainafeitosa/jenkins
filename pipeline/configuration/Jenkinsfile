agent {
    on_merge to: develop_branch, {
        continuous_integration()
        // helm install && helm lint && helm template
        // deploy_to dev
    }

    on_commit to: release_branch, {
        continuous_integration()
        // helm install && helm package && helm push
        // deploy_to test
    }

    // on_merge to: master, from: release, {
    //     approve
    //     deploy_to prod
    // }
}